# Colony harness optional research dependencies.
#
# The core harness is mostly stdlib-only. These packages are needed for
# native CAMEL research scouts and CAMEL SearchToolkit web search.

camel-ai==0.2.90
ddgs==9.14.4
fastapi==0.115.6
uvicorn[standard]==0.34.0
web3>=6.0.0
eth-account>=0.10.0
